language: java

jdk:
  - openjdk8

os:
  - linux

branches:
  only:
    - travis

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

before_script:
  - git clone git@github.com:sg4e/FF4StatsLib.git ../FF4StatsLib
